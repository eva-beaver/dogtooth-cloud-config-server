server:
  port: 9001

spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/eva-beaver/dogtooth-config
          clone-on-start: true